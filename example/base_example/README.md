# Easy Translate Base Example

A simple example program with Qt.

## Build And Install

Make sure your Qt can be found.

```shell
cmake -B build
cmake --build build -j
cmake --install build --prefix .
```
